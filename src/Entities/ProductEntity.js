export default class ProductEntity {
    constructor(id, quantity, size, color, price) {
        this.id = id;
        this.quantity = quantity;
        this.size = size;
        this.color = color;
        this.price = price;
    }
}